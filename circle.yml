machine:
  node:
    version: v8.4.0
test:
  override:
    - npm i ethjs-abi
    - npm test
  post:
    -  bash <(curl -s https://codecov.io/bash)
    -  npm run build:dist && npm run build:dist:prod
