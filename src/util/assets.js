// TODO is there a better way to load SVG assets into a js bundle?

const logo = ''
const appleApp = ''
const androidApp = ''
const close = ''

const SVG = {
  logo,
  appleApp,
  androidApp,
  close
}

export default SVG
